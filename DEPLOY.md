# عيادة تحدث — دليل النشر (GitHub + Supabase + Vercel)

هذا الإصدار مصمَّم خصيصًا حتى لا تتعامل أنت (أو الأدمن) مع سوبابيس مباشرة أبدًا
بعد الإعداد الأول. كل التواصل مع قاعدة البيانات يمر فقط عبر كود الموقع، بمفتاح
سرّي محفوظ في فيرسيل ولا يظهر لأحد.

---

## الخطوة ١: Supabase (قاعدة البيانات + تخزين الصور)

1. أنشئ حسابًا في [supabase.com](https://supabase.com) → **New Project**.
2. بعد اكتمال إنشاء المشروع، افتح **SQL Editor** من القائمة الجانبية → **New query**.
3. افتح ملف `supabase-schema.sql` المرفق، انسخ **محتواه بالكامل**، الصقه، واضغط **Run**.
   - هذا ينشئ كل الجداول، ويضيف بيانات الموقع الافتراضية، وينشئ حساب الدخول الأول.
4. من القائمة الجانبية اذهب إلى **Storage** → **New bucket**:
   - الاسم: `uploads` (بالضبط بهذا الاسم — الكود يعتمد عليه)
   - فعّل **Public bucket** ✅
5. من القائمة الجانبية اذهب إلى **Project Settings → API**. ستحتاج قيمتين فقط:
   - **Project URL**
   - **service_role key** (وليس anon key — هذا المفتاح سرّي جدًا، لا تشاركه مع أحد ولا تضعه في أي مكان عام)

⚠️ لن تحتاج تفتح لوحة سوبابيس بعد هذه الخطوة أبدًا في الاستخدام اليومي.

---

## الخطوة ٢: GitHub

1. أنشئ حسابًا في [github.com](https://github.com) إن لم يكن لديك.
2. أنشئ **مستودعًا جديدًا (New repository)** — خاص أو عام، حسب رغبتك.
3. ارفع كل ملفات هذا المشروع إليه (اسحب المجلد كاملًا عبر واجهة GitHub في المتصفح: "Add file → Upload files"، أو باستخدام Git إن كنت تعرفه).

---

## الخطوة ٣: Vercel

1. أنشئ حسابًا في [vercel.com](https://vercel.com) — الأسهل: سجّل الدخول مباشرة بحساب GitHub.
2. اضغط **Add New → Project** واختر المستودع الذي رفعته للتو.
3. **قبل الضغط على Deploy**، افتح قسم **Environment Variables** وأضف ثلاث قيم بالضبط:

   | الاسم | القيمة |
   |---|---|
   | `SUPABASE_URL` | القيمة التي نسخته من سوبابيس (الخطوة ١-٥) |
   | `SUPABASE_SERVICE_ROLE_KEY` | مفتاح service_role الذي نسخته |
   | `SESSION_SECRET` | أي نص عشوائي طويل من عندك (مثال: `x7Kp9mQ2vL8nR4tY6wZ1sA3dF5gH0jK`) — لا داعي أن يكون من مكان معيّن، فقط اختر نصًا طويلًا وعشوائيًا واحفظه |

4. اضغط **Deploy**. بعد دقيقة تقريبًا سيصبح موقعك شغّالًا على رابط مثل:
   `https://your-project.vercel.app`

---

## الخطوة ٤: أول دخول للوحة التحكم

- الرابط: `https://your-project.vercel.app/admin`
- البريد: `admin@tahaduth-clinic.sa`
- كلمة المرور المؤقتة: `ChangeMe123!`

سيُطلب منك تغييرها فورًا عند أول دخول.

---

## دومين خاص بك (اختياري)

من صفحة مشروعك في Vercel → **Settings → Domains** → أضف الدومين الذي تملكه، واتبع
التعليمات الظاهرة لتحديث إعدادات DNS من مزوّد الدومين (مثل GoDaddy أو أي جهة أخرى).
Vercel يشرح لك بالضبط أي سجل (Record) تضيفه — عادة يكون الأمر نسخ ولصق سطر واحد.

---

## ماذا لو احتجت تعدّل الكود لاحقًا؟

أي تعديل على ملفات المشروع، ثم رفعه لنفس مستودع GitHub (Commit جديد)، سيجعل
Vercel **يعيد النشر تلقائيًا خلال ثوانٍ** — لا حاجة لأي خطوة يدوية إضافية.

---

## الفرق عن النسخة السابقة (Node محلي)

| | النسخة المحلية | هذه النسخة |
|---|---|---|
| التخزين | ملفات JSON على القرص | جداول Supabase |
| الصور | مجلد `uploads/` محلي | Supabase Storage |
| التشغيل | يتطلب `node server.js` دائمًا | يعمل تلقائيًا وباستمرار على Vercel |
| الحاجة لجهازك | نعم، طول الوقت | لا إطلاقًا بعد النشر |

كل الوظائف (الحجز، العداد التنازلي بـ٢٠ دقيقة، الأدمن، الحسابات المتعددة) **متطابقة تمامًا** — فقط طريقة التخزين اختلفت.
